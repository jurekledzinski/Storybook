import { Meta, Story } from '@storybook/addon-docs/blocks';
import { faCheckCircle } from '@fortawesome/free-solid-svg-icons';

import * as IconStories from './Icon.stories.ts';

<Meta of={IconStories} />

### Icon Component

The `Icon` component is used for display icon. It can be customized using various props.

#### Props (typescript)

Here’s a list of the props for the `Icon` component:

```js
{
  icon: IconProp;
  alt?: string;
  className?: string;
  color?: 'info' | 'negative' | 'primary' | 'secondary' | 'success' |'warning';
  mirrored?: boolean;
  size?: string | number;
  weight?: IconWeight;
}
```

#### Example usage `Icon`

```tsx
<Icon icon={faCheckCircle} />
```

#### Preview `Icon`

<Story of={IconStories.Default} />
