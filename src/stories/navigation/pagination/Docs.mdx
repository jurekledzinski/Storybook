import { Meta, Story } from '@storybook/addon-docs/blocks';

import PaginationMeta, * as PaginationStories from './Pagination.stories';

<Meta of={PaginationMeta} />

### Pagination Component

The `Pagination` component allows users to navigate between pages of content.

#### Props (typescript)

Here’s a list of the props for the `Pagination` component:

By `className` can add:

- border: 'b-xs' | 'b-sm' | 'b-md' | 'b-lg';
- radius: 'r-xs' | 'r-sm' | 'r-md' | 'r-lg' | 'r-xl';

```js
{
  className?:string;
  children?: React.ReactNode;
  color?: 'primary' | 'secondary';
  size?: "size-xxs" | "size-xs" | "size-sm" | "size-md" | "size-lg";
  spacing?: "none" | "tight" | "normal" | "loose" | "extra-loose";
  variant?: "contained" | "outlined" | "text";
  totalPages: number;
  maxRange?: number;
  perPage?: number[];
}
```

#### Example Usage `Pagination`

```tsx
<Pagination size="size-sm" spacing="tight" totalPages={10} variant="outlined">
  <PaginationArrow id="first" label={faAnglesLeft} onClick={() => {}} disabled={false} />
  <PaginationArrow id="prev" label={faChevronLeft} onClick={() => {}} disabled={false} />
  <PaginationInfo indexStart={1} indexEnd={10} totalAmount={100} />
  <PaginationArrow id="next" label={faChevronRight} onClick={() => {}} disabled={false} />
  <PaginationArrow id="last" label={faAnglesRight} onClick={() => {}} disabled={false} />
</Pagination>
```

#### Preview `Pagination`

<Story of={PaginationStories.Default} />
